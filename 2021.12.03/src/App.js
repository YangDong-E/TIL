import React from "react";
import MyComponent from "./MyComponent"; // MyComponent를 불러와서 사용한다. (모듈 불러오기)

const App = () => {
  return <MyComponent name="React" />; //props값 직접 넣어주기
};

export default App;
